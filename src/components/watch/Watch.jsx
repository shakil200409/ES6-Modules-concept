import React from "react";
import { add, multiply } from "../../Utils/calculate/Calculate";
// import add from "../../Utils/calculate/Calculate";

const sum = add(10, 20);
console.log(sum);

const mult = multiply(5, 10);
console.log(mult);

const Watch = () => {
  return <div></div>;
};

export default Watch;
